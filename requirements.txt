# Video2VisionDoc 依赖
# ---------------------------------------------------------------
# 推荐流水线 video2visiondoc/ 的最小依赖（按使用阶段标注）：
#   下载:    requests + 系统 ffmpeg
#   转写:    faster-whisper
#   关键帧:  Pillow + numpy + 系统 ffmpeg
#   翻译:    requests（OpenAI 兼容 API）
#   文档:    标准库（可选 playwright 输出 PDF）
# ---------------------------------------------------------------
requests>=2.31.0
faster-whisper>=1.0.0
Pillow>=10.2.0
numpy>=1.26.0

# 可选：PDF 输出（推荐流水线 --pdf）
# playwright>=1.40.0

# ===== 以下为初版结构（main.py + src/）的补充依赖 =====
# 视频下载
yt-dlp>=2024.12.0

# 音频处理
ffmpeg-python>=0.2.0
pydub>=0.25.1

# 语音转文字
openai-whisper>=20231117

# 翻译
deep-translator>=1.11.4
openai>=1.0.0

# 图像处理与关键帧提取
opencv-python>=4.9.0
Pillow>=10.2.0
scikit-image>=0.22.0

# 文档生成
jinja2>=3.1.3
markdown>=3.5.2
weasyprint>=61.2

# 配置与工具
pyyaml>=6.0.1
tqdm>=4.66.2
python-dotenv>=1.0.1
click>=8.1.7

# 可选：GPU加速
# torch>=2.1.0
# torchaudio>=2.1.0
